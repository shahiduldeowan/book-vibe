import { HashLoader } from "react-spinners";

const Loader = () => {
  return (
    <div className=" w-full flex justify-center items-start">
      <HashLoader color="#23BE0A" size={60} />
    </div>
  );
};

export default Loader;
